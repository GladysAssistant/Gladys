/**
 * @description Get MQTT configuration.
 * @returns {Object} Current MQTT network configuration.
 * @example
 * getConfiguration();
 */
function getConfiguration() {
  const configuration = {};
  return configuration;
}

module.exports = {
  getConfiguration,
};
