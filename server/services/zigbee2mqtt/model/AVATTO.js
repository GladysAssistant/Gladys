const { features } = require('../utils/features');

/**
 * AVATTO managed models.
 */
const AVATTO = {
  brand: 'AVATTO',
  models: {
    'ZGB-WS-EU': [features.switch_sensor],
  },
};

module.exports = {
  AVATTO,
};
