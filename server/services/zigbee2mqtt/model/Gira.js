const { features } = require('../utils/features');

/**
 * Gira managed models.
 */
const Gira = {
  brand: 'Gira',
  models: {
    '2430-100': [features.switch_sensor],
  },
};

module.exports = {
  Gira,
};
