const { features } = require('../utils/features');

/**
 * Gira managed models.
 */
const Gira = {
  brand: 'Gira',
  models: {
    '2430-100': [features.button],
    '2435-10': [features.button],
  },
};

module.exports = {
  Gira,
};
