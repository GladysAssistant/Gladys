const { features } = require('../utils/features');

/**
 * Nedis managed models.
 */
const Nedis = {
  brand: 'Nedis',
  models: {
    ZBRC10WT: [features.button],
  },
};

module.exports = {
  Nedis,
};
