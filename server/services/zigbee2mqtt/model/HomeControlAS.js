const { features } = require('../utils/features');

/**
 * Home Control AS managed models.
 */
const HomeControlAS = {
  brand: 'Home Control AS',
  models: {
    'HC-SLM-1': [features.door],
  },
};

module.exports = {
  HomeControlAS,
};
