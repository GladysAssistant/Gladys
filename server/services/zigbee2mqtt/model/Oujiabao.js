const { features } = require('../utils/features');

/**
 * Oujiabao managed models.
 */
const Oujiabao = {
  brand: 'Oujiabao',
  models: {
    'CR701-YZ': [features.smoke, features.smoke],
  },
};

module.exports = {
  Oujiabao,
};
