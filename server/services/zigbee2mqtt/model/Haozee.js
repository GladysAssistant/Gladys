const { features } = require('../utils/features');

/**
 * Haozee managed models.
 */
const Haozee = {
  brand: 'Haozee',
  models: {
    'ESW-OZAA-EU': [features.button],
  },
};

module.exports = {
  Haozee,
};
