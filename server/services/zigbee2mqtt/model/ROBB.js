const { features } = require('../utils/features');

/**
 * ROBB managed models.
 */
const ROBB = {
  brand: 'ROBB',
  models: {
    'ROB_200-004-0': [features.switch, features.brightness],
  },
};

module.exports = {
  ROBB,
};
