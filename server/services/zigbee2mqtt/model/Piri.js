const { features } = require('../utils/features');

/**
 * Piri managed models.
 */
const Piri = {
  brand: 'Piri',
  models: {
    HSIO18008: [features.smoke],
  },
};

module.exports = {
  Piri,
};
