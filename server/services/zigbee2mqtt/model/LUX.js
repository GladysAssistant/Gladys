const { features } = require('../utils/features');

/**
 * LUX managed models.
 */
const LUX = {
  brand: 'LUX',
  models: {
    'KN-Z-WH1-B04': [features.temperature],
  },
};

module.exports = {
  LUX,
};
