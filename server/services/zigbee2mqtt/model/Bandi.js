const { features } = require('../utils/features');

/**
 * Bandi managed models.
 */
const Bandi = {
  brand: 'Bandi',
  models: {
    BDS03G1: [features.switch_sensor],
  },
};

module.exports = {
  Bandi,
};
