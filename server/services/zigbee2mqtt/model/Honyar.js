const { features } = require('../utils/features');

/**
 * Honyar managed models.
 */
const Honyar = {
  brand: 'Honyar',
  models: {
    U86K31ND6: [features.switch_sensor],
  },
};

module.exports = {
  Honyar,
};
