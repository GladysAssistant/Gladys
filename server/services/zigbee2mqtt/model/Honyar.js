const { features } = require('../utils/features');

/**
 * Honyar managed models.
 */
const Honyar = {
  brand: 'Honyar',
  models: {
    U86K31ND6: [features.switch],
  },
};

module.exports = {
  Honyar,
};
