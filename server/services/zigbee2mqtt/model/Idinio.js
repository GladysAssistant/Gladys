const { features } = require('../utils/features');

/**
 * Idinio managed models.
 */
const Idinio = {
  brand: 'Idinio',
  models: {
    '0140302': [features.brightness, features.light],
  },
};

module.exports = {
  Idinio,
};
