const { features } = require('../utils/features');

/**
 * IOTPerfect managed models.
 */
const IOTPerfect = {
  brand: 'IOTPerfect',
  models: {
    'PF-PM02D-TYZ': [features.switch_sensor],
  },
};

module.exports = {
  IOTPerfect,
};
