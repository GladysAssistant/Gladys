const { features } = require('../utils/features');

/**
 * Danalock managed models.
 */
const Danalock = {
  brand: 'Danalock',
  models: {
    'V3-BTZB/V3-BTZBE': [features.door],
  },
};

module.exports = {
  Danalock,
};
