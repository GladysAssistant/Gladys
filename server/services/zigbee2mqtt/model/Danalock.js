const { features } = require('../utils/features');

/**
 * Danalock managed models.
 */
const Danalock = {
  brand: 'Danalock',
  models: {
    'V3-BTZB': [features.door],
  },
};

module.exports = {
  Danalock,
};
