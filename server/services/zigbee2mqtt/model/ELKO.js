const { features } = require('../utils/features');

/**
 * ELKO managed models.
 */
const ELKO = {
  brand: 'ELKO',
  models: {
    '316GLEDRF': [features.switch, features.brightness],
  },
};

module.exports = {
  ELKO,
};
