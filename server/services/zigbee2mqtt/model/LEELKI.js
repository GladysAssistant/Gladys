const { features } = require('../utils/features');

/**
 * LEELKI managed models.
 */
const LEELKI = {
  brand: 'LEELKI',
  models: {
    'WP33-EU': [features.switch],
  },
};

module.exports = {
  LEELKI,
};
