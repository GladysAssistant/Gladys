const { features } = require('../utils/features');

/**
 * EasyAccess managed models.
 */
const EasyAccess = {
  brand: 'EasyAccess',
  models: {
    'EasyCode903G2.1': [features.button, features.door],
  },
};

module.exports = {
  EasyAccess,
};
