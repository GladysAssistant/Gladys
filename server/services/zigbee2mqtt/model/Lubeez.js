const { features } = require('../utils/features');

/**
 * Lubeez managed models.
 */
const Lubeez = {
  brand: 'Lubeez',
  models: {
    '12AB': [features.brightness, features.light],
  },
};

module.exports = {
  Lubeez,
};
