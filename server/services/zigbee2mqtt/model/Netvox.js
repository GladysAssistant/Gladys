const { features } = require('../utils/features');

/**
 * Netvox managed models.
 */
const Netvox = {
  brand: 'Netvox',
  models: {
    Z809A: [features.switch, features.power],
  },
};

module.exports = {
  Netvox,
};
