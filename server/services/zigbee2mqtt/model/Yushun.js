const { features } = require('../utils/features');

/**
 * Yushun managed models.
 */
const Yushun = {
  brand: 'Yushun',
  models: {
    'YS-MT750': [features.door],
  },
};

module.exports = {
  Yushun,
};
