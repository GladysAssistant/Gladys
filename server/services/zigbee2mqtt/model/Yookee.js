const { features } = require('../utils/features');

/**
 * Yookee managed models.
 */
const Yookee = {
  brand: 'Yookee',
  models: {
    D10110: [features.door],
  },
};

module.exports = {
  Yookee,
};
