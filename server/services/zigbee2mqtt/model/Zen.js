const { features } = require('../utils/features');

/**
 * Zen managed models.
 */
const Zen = {
  brand: 'Zen',
  models: {
    'Zen-01-W': [features.temperature],
  },
};

module.exports = {
  Zen,
};
