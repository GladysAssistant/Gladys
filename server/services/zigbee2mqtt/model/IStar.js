const { features } = require('../utils/features');

/**
 * iStar managed models.
 */
const IStar = {
  brand: 'iStar',
  models: {
    'SCCV2401-1': [features.brightness, features.light],
  },
};

module.exports = {
  IStar,
};
