const { features } = require('../utils/features');

/**
 * Current Products Corp managed models.
 */
const CurrentProductsCorp = {
  brand: 'Current Products Corp',
  models: {
    'CP180335E-01': [features.door],
  },
};

module.exports = {
  CurrentProductsCorp,
};
