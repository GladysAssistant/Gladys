const { features } = require('../utils/features');

/**
 * Brimate managed models.
 */
const Brimate = {
  brand: 'Brimate',
  models: {
    'FZB8708HD-S1': [features.motion],
  },
};

module.exports = {
  Brimate,
};
