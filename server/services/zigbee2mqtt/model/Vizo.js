const { features } = require('../utils/features');

/**
 * Vizo managed models.
 */
const Vizo = {
  brand: 'Vizo',
  models: {
    'VZ-222S': [features.switch],
  },
};

module.exports = {
  Vizo,
};
