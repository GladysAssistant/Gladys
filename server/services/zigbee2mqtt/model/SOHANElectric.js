const { features } = require('../utils/features');

/**
 * SOHAN Electric managed models.
 */
const SOHANElectric = {
  brand: 'SOHAN Electric',
  models: {
    'RDCBC/Z': [features.switch_sensor],
  },
};

module.exports = {
  SOHANElectric,
};
