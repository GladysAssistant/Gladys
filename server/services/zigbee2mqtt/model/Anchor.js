const { features } = require('../utils/features');

/**
 * Anchor managed models.
 */
const Anchor = {
  brand: 'Anchor',
  models: {
    '67200BL': [features.switch_sensor],
  },
};

module.exports = {
  Anchor,
};
