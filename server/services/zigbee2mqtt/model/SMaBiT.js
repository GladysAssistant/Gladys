const { features } = require('../utils/features');

/**
 * SMaBiT managed models.
 */
const SMaBiT = {
  brand: 'SMaBiT',
  models: {
    'AV2010/21C': [features.door],
  },
};

module.exports = {
  SMaBiT,
};
