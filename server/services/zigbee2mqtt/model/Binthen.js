const { features } = require('../utils/features');

/**
 * Binthen managed models.
 */
const Binthen = {
  brand: 'Binthen',
  models: {
    BCM100D: [features.door],
    CV01A: [features.door],
  },
};

module.exports = {
  Binthen,
};
