const { features } = require('../utils/features');

/**
 * eCozy managed models.
 */
const ECozy = {
  brand: 'eCozy',
  models: {
    '1TST-EU': [features.temperature],
  },
};

module.exports = {
  ECozy,
};
