const { features } = require('../utils/features');

/**
 * CREE managed models.
 */
const CREE = {
  brand: 'CREE',
  models: {
    B00TN589ZG: [features.brightness, features.light],
  },
};

module.exports = {
  CREE,
};
