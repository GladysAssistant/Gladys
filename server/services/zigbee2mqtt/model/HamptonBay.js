const { features } = require('../utils/features');

/**
 * Hampton Bay managed models.
 */
const HamptonBay = {
  brand: 'Hampton Bay',
  models: {
    '99432': [features.switch_sensor],
  },
};

module.exports = {
  HamptonBay,
};
