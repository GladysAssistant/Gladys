const { features } = require('../utils/features');

/**
 * Mycket managed models.
 */
const Mycket = {
  brand: 'Mycket',
  models: {
    'MS-SP-LE27WRGB': [features.brightness, features.color, features.color_temperature, features.light],
  },
};

module.exports = {
  Mycket,
};
