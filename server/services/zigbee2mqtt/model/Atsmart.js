const { features } = require('../utils/features');

/**
 * Atsmart managed models.
 */
const Atsmart = {
  brand: 'Atsmart',
  models: {
    Z6: [features.switch_sensor],
  },
};

module.exports = {
  Atsmart,
};
