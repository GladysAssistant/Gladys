const { features } = require('../utils/features');

/**
 * Atsmart managed models.
 */
const Atsmart = {
  brand: 'Atsmart',
  models: {
    Z6: [features.switch],
  },
};

module.exports = {
  Atsmart,
};
