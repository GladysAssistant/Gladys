const { features } = require('../utils/features');

/**
 * Third Reality managed models.
 */
const ThirdReality = {
  brand: 'Third Reality',
  models: {
    '3RSS008Z': [features.switch_sensor],
  },
};

module.exports = {
  ThirdReality,
};
