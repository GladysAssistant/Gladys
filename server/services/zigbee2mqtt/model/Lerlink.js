const { features } = require('../utils/features');

/**
 * Lerlink managed models.
 */
const Lerlink = {
  brand: 'Lerlink',
  models: {
    X706U: [features.brightness, features.light],
  },
};

module.exports = {
  Lerlink,
};
