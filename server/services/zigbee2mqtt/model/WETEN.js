const { features } = require('../utils/features');

/**
 * WETEN managed models.
 */
const WETEN = {
  brand: 'WETEN',
  models: {
    '1GNNTS': [features.switch_sensor],
  },
};

module.exports = {
  WETEN,
};
