const { features } = require('../utils/features');

/**
 * AXIS managed models.
 */
const AXIS = {
  brand: 'AXIS',
  models: {
    'GR-ZB01-W': [features.door], // curtain / shuttle
  },
};

module.exports = {
  AXIS,
};
