const { features } = require('../utils/features');

/**
 * ilux managed models.
 */
const ilux = {
  brand: 'ilux',
  models: {
    '900008-WW': [features.switch, features.brightness],
  },
};

module.exports = {
  ilux,
};
