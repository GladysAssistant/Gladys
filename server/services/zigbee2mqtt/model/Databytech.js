const { features } = require('../utils/features');

/**
 * databyte.ch managed models.
 */
const Databytech = {
  brand: 'databyte.ch',
  models: {
    // DTB190502A1: [],
    'ED2004-012': [features.switch],
  },
};

module.exports = {
  Databytech,
};
