const { features } = require('../utils/features');

/**
 * Benexmart managed models.
 */
const Benexmart = {
  brand: 'Benexmart',
  models: {
    'ZM-sui1': [features.button],
  },
};

module.exports = {
  Benexmart,
};
