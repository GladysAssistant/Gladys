const { features } = require('../utils/features');

/**
 * CY-LIGHTING managed models.
 */
const CYLIGHTING = {
  brand: 'CY-LIGHTING',
  models: {
    'DM A60F': [features.brightness, features.light],
  },
};

module.exports = {
  CYLIGHTING,
};
