const { features } = require('../utils/features');

/**
 * ilux managed models.
 */
const Ilux = {
  brand: 'ilux',
  models: {
    '900008-WW': [features.brightness, features.light],
  },
};

module.exports = {
  Ilux,
};
