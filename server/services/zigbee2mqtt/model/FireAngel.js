const { features } = require('../utils/features');

/**
 * FireAngel managed models.
 */
const FireAngel = {
  brand: 'FireAngel',
  models: {
    'W2-Module': [features.smoke],
  },
};

module.exports = {
  FireAngel,
};
