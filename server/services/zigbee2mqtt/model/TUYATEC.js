const { features } = require('../utils/features');

/**
 * TUYATEC managed models.
 */
const TUYATEC = {
  brand: 'TUYATEC',
  models: {
    RH3040: [features.presence],
  },
};

module.exports = {
  TUYATEC,
};
