const { features } = require('../utils/features');

/**
 * Trio2sys managed models.
 */
const Trio2sys = {
  brand: 'Trio2sys',
  models: {
    '20020002': [features.button],
  },
};

module.exports = {
  Trio2sys,
};
