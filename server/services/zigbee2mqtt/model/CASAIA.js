const { features } = require('../utils/features');

/**
 * CASAIA managed models.
 */
const CASAIA = {
  brand: 'CASAIA',
  models: {
    'CTHS-317-ET': [features.temperature],
  },
};

module.exports = {
  CASAIA,
};
