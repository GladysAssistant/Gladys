const { features } = require('../utils/features');

/**
 * Vrey managed models.
 */
const Vrey = {
  brand: 'Vrey',
  models: {
    'VR-X712U-0013': [features.switch],
  },
};

module.exports = {
  Vrey,
};
