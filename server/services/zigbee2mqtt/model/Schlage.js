const { features } = require('../utils/features');

/**
 * Schlage managed models.
 */
const Schlage = {
  brand: 'Schlage',
  models: {
    BE468: [features.door],
  },
};

module.exports = {
  Schlage,
};
