const { features } = require('../utils/features');

/**
 * Norklmes managed models.
 */
const Norklmes = {
  brand: 'Norklmes',
  models: {
    'MKS-CM-W5': [features.switch],
  },
};

module.exports = {
  Norklmes,
};
