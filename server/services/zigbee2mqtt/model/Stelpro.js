const { features } = require('../utils/features');

/**
 * Stelpro managed models.
 */
const Stelpro = {
  brand: 'Stelpro',
  models: {
    ST218: [features.temperature],
  },
};

module.exports = {
  Stelpro,
};
