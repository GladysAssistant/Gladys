const { features } = require('../utils/features');

/**
 * ORVIBO managed models.
 */
const ORVIBO = {
  brand: 'ORVIBO',
  models: {
    SN10ZW: [features.motion],
  },
};

module.exports = {
  ORVIBO,
};
