const { features } = require('../utils/features');

/**
 * Mumubiz managed models.
 */
const Mumubiz = {
  brand: 'Mumubiz',
  models: {
    'ZJSB9-80Z': [features.switch],
  },
};

module.exports = {
  Mumubiz,
};
