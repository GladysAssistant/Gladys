const { features } = require('../utils/features');

/**
 * Weiser managed models.
 */
const Weiser = {
  brand: 'Weiser',
  models: {
    '9GED18000-009': [features.door],
    '9GED21500-005': [features.door],
  },
};

module.exports = {
  Weiser,
};
