const { features } = require('../utils/features');

/**
 * ROOME managed models.
 */
const ROOME = {
  brand: 'ROOME',
  models: {
    'HSC1-WD-0': [features.door],
  },
};

module.exports = {
  ROOME,
};
