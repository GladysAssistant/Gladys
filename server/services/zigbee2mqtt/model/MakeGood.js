const { features } = require('../utils/features');

/**
 * MakeGood managed models.
 */
const MakeGood = {
  brand: 'MakeGood',
  models: {
    'MG-ZG04W/B/G': [features.switch_sensor],
  },
};

module.exports = {
  MakeGood,
};
