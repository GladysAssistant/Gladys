const { DEVICE_FEATURE_CATEGORIES, DEVICE_FEATURE_TYPES } = require('../../../../utils/constants');

const c2cCamera2 = {
  value: 'c2c-camera-2',
  category: DEVICE_FEATURE_CATEGORIES.CAMERA,
  types: [DEVICE_FEATURE_TYPES.CAMERA.IMAGE],
};

module.exports = { c2cCamera2 };
