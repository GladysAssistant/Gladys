const { list } = require('./device-profile.list');
const { create } = require('./device-profile.create');
const { destroy } = require('./device-profile.destroy');

module.exports = {
  list,
  create,
  destroy,
};
