const TIMERS = {
  SCAN: 5000,
  CONNECT: 5000,
  DISCOVER: 2000,
  READ: 2000,
  WRITE: 2000,
};

const PARAMS = {
  CONNECTABLE: 'connectable',
  LOADED: 'loaded',
  MANUFACTURER: 'manufacturer',
};

module.exports = { TIMERS, PARAMS };
