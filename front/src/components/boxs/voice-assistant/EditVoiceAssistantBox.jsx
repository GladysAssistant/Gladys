import BaseEditBox from '../baseEditBox';

const EditVoiceAssistantBox = props => {
  return <BaseEditBox {...props} titleKey="dashboard.boxTitle.voice-assistant" />;
};

export default EditVoiceAssistantBox;
