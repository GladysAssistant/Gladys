import { Text } from 'preact-i18n';
import { Link } from 'preact-router/match';
import cx from 'classnames';

const EventLogPage = ({children,...props }) => (
    <div>coucou</div>);

export default EventLogPage;