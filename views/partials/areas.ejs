<!-- NEW BOX -->
<div class="box box-default" data-ng-controller="AreaCtrl as vm">
    <div class="box-header with-border">
        <h3 class="box-title"><%= __('area-box-title') %> <small ng-show="vm.saving"><%= __('area-box-saving') %></small></h3>
    </div> 
        <div class="box-body table-responsive" >
            <p>
            <%= __('area-box-description') %>
            </p>
            <table id="example2" class="table table-bordered table-hover">
                <thead>
                    <tr>
                        <th><%= __('area-name') %></th>
                        <th><%= __('area-latitude') %></th>
                        <th><%= __('area-longitude') %></th>
                        <th><%= __('area-radius') %></th>
                        <th><%= __('area-action') %></th>
                    </tr>
                </thead>
                <tbody>
                    <tr ng-repeat="area in vm.areas" class="ng-cloak">
                        <td><input type="text" ng-model="area.name" ng-model-options='{ debounce: 500 }' ng-change="vm.updateArea(area.id, area);" class="form-control" /></td>
                        <td><input type="text" ng-model="area.latitude" ng-model-options='{ debounce: 500 }' ng-change="vm.updateArea(area.id, area);" class="form-control" /></td>
                        <td><input type="text" ng-model="area.longitude" ng-model-options='{ debounce: 500 }' ng-change="vm.updateArea(area.id, area);" class="form-control" /></td>
                        <td><input type="text" ng-model="area.radius" ng-model-options='{ debounce: 500 }' ng-change="vm.updateArea(area.id, area);" class="form-control" /></td>
                        <td><button ng-click="vm.deleteArea($index, area.id)" class= "btn btn-danger btn-sm"><%= __('area-delete') %></button></td>
                    </tr>
                    <tr>
                        <td><input type="text" class="form-control" ng-model="vm.newArea.name" data-toggle="tooltip" data-original-title="Donnez un nom à votre zone" /></td>
                        <td><input type="text" class="form-control" ng-model="vm.newArea.latitude" data-toggle="tooltip" data-original-title="Entrez la latitude par ex. 42.6542" /></td>
                        <td><input type="text" class="form-control" ng-model="vm.newArea.longitude" data-toggle="tooltip" data-original-title="Entrez la longitude par ex. 4.5482" /></td>
                        <td><input type="text" class="form-control" ng-model="vm.newArea.radius" data-toggle="tooltip" data-original-title="Entrez un rayon en mètres par ex. 25" /></td>
                        <td><button class="btn btn-success btn-sm" ng-click="vm.createArea(vm.newArea)"><%= __('area-new') %></button></td>
                    </tr>
                </tbody>
            </table>

        </div>  
</div>
