<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title><%= __('Sign in') %></title>
        <meta content='width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no' name='viewport'>
        <link href="/styles/dependencies/bootstrap.min.css" rel="stylesheet" type="text/css" />
        <link rel="stylesheet" href="/styles/dependencies/font-awesome.min.css">
        <!-- Theme style -->
        <link href="/styles/dependencies/adminLTE/AdminLTE.min.css" rel="stylesheet" type="text/css" />
        <link href="/styles/login.css" rel="stylesheet" type="text/css" />

    </head>
    <body>

        <div class="login-box" id="login-box">
            <% if(!done) { %>
            <form action="/forgotpassword" method="post">
                <div class="login-box-body">
                    <div class="login-box-msg text-green"><h2><%= __('forgot-password-title') %></h2></div>

                    <% if(resetPasswordFailed) { %>
                        <p class="bg-danger" style="padding:15px;"><%= __('forgot-password-reset-failed') %></p>
                    <% } %>

                    <div class="form-group">
                        <input type="text" name="email" class="form-control" placeholder="<%= __('Email') %>"/>
                    </div>
                    
                    <div class="footer">                                                               
                        <button type="submit" class="btn btn-flat btn-success btn-block"><%= __('forgot-password-button') %></button>  
                    </div>
                </div>
                <div class="footer"> 
                
                </div>
            </form>
            <% } else { %>
                <div class="login-box-body">
                        <%= __('forgot-password-done-text') %>
                </div>
            <% } %>

        </div>

        <script src="/js/dependencies/jquery.min.js"></script>
        <script src="/js/dependencies/bootstrap.min.js" type="text/javascript"></script>

    </body>
</html>